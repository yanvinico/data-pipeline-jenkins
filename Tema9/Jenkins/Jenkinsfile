pipeline {
    agent any
    stages {
    
        stage('Build the environment'){
            steps {
                sh '''
                sudo ${WORKSPACE}/Script/sh/build.sh
                '''
            }
        }
        stage('Deploy') {
            steps {
                sh '''
                sudo ${WORKSPACE}/Script/sh/deploy.sh
                '''
            }
        }
        stage('Sync') {
            steps {
                sh '''
                sudo ${WORKSPACE}/Script/sh/sync.sh
                '''
            }
        }
    }
    post {
        always {
            cleanWs()
        }
     
    }
}